<div class="span3 process-nav">
   <div class="well sidebar-nav">
     
     <% if current_user.is_special_user? %>
       <%= get_process_nav( :company_admin_management , params ).html_safe %> 
     <% end %>
     
     
     
     <% if current_user.has_role?(:company_admin) %>
     <!-- Premium user has access to all features -->
       <%= get_process_nav( :project_setup , params ).html_safe %> 
       <%= get_process_nav( :project_management , params ).html_safe %> 
       <%= get_process_nav( :collaboration , params ).html_safe %> 
       
       
       <% if HYPER_PREMIUM_DEPLOYMENT %>
         <%#= get_process_nav( :marketing , params ).html_safe %> 
       <% end %>
     <% end %>
     
     
     <% if ( current_user.has_role?(:standard) ) and not (current_user.has_role?(:company_admin)   )  %>
     
     
     <!-- Standard user can only collaborate (passive), can't create project -->
     <!--  depending on the project membership role, it can have: previledges like upload picture ( collaborator) 
     or just commenter (client) -->
       <%= get_process_nav( :collaboration , params ).html_safe %> 
       <%#= get_process_nav( :submission_grading , params ).html_safe %> 
     <% end %>
    
     
     <%# if current_user.has_role?(:student) %>
        <%#= get_process_nav( :student , params ).html_safe %> 
      <%# end %>
     
     
     <!-- <ul class="nav nav-list">

       <li class="nav-header">School Admin</li>
       <li>Add Student</li>
       <li>Add Teacher</li>


            
     </ul> -->
   </div><!--/.well -->
 </div><!--/span-->